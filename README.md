A largely vibe-coded implementation of the algorithm in https://arxiv.org/pdf/2603.08807, as well as:

(1) Some optimizations to (try to) speed it up,
(2) A small generalization to directed trees with time stamps.
